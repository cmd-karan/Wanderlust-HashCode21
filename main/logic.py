"""
sort by time to reach destination (irrespect of the wait)
take the least one.
and make it reach to the dest.(can delay if less than the duration.)

for every
Node: cars present on it
what is the next node.

1,2,3  - 1
first give in queue if all endNodes done then schedule is fixed.
can update schedule (but will have to recompute) - to add the wait time.


Question?

find out all the 
"""
class Logic:
    def sortByTime(self,):

    def computation(self, simulationTime, intersectionCount, streetCount, scorePerCar, graph, edgeToNode, nodeToEdge, carIDMap):
    

    